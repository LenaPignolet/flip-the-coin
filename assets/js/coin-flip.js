const flipImage = document.querySelector('');
const flipBtn = document.querySelector('game_btn');

flipBtn.addEventListener('click', () => {
    const result = Math.random() < 0.5 ? "Heads" : "Tails";


})